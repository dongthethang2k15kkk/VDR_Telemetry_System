# === RENDER WORKER (NAC 1 - khung, chua render that) ===
# Nac 2 se them cv2/ffmpeg/pandas + overlay_engine.
FROM python:3.11-slim
WORKDIR /app
COPY render_worker.py ./
ENV PYTHONUNBUFFERED=1
CMD ["python", "render_worker.py"]
